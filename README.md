# Money Manager

A local-first React expense tracker for accounts, credit cards, debt, spending, budgets, and cashback estimates.

## Run locally

```bash
npm install
npm run dev
```

Open the URL Vite prints, usually `http://localhost:5173`.
